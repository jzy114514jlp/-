# 别漏事：给现场展示伙伴的操作指南

这个项目可在你的电脑上本地运行，不需要登录账号。请在出发前完成安装和排练；第一次安装依赖、第一次下载截图识字模型时需要网络。

## 1. 准备电脑

安装 Node.js **22.13 或更高版本**。打开终端，运行 `node --version`，确认版本号。Windows 可以使用“命令提示符”或 PowerShell；macOS 可以使用“终端”。

打开 [项目仓库](https://github.com/jzy114514jlp/The-road-of-grow)，点击 **Code → Download ZIP**，解压到电脑上。若已经安装 Git，也可以运行：

```bash
git clone https://github.com/jzy114514jlp/The-road-of-grow.git
```

## 2. 启动网页

**Windows：**进入解压后的项目文件夹，双击 `start-demo.cmd`。第一次运行会自动安装依赖，可能需要几分钟。保持出现的终端窗口打开，在浏览器访问窗口显示的地址，通常是 <http://localhost:5173/>。

**macOS / Linux：**在终端进入项目文件夹，运行：

```bash
npm ci
npm run dev
```

然后打开终端显示的 Local 地址，通常是 <http://localhost:5173/>。如果端口被占用，请以终端实际显示的地址为准。

## 3. 提前排练

1. 点击“试用黑客松通知”，展示从通知提取的时间、地点、报名方式和待办。
2. 指出原文没有报名截止日期，因此工具显示“未提及”，不会编造日期。
3. 编辑一个字段，勾选一项待办，再导出日历文件。
4. 如果要演示截图识字，请在联网时用**同一台电脑和浏览器**预先试一次，并准备一张清晰截图。

现场网络不稳定时，可以用内置样例或粘贴通知文字演示。浏览器页面中的通知和图片在本地处理；截图识字首次使用可能需要联网下载模型。

## 常见问题

- 提示找不到 `node` 或 `npm`：安装或更新 Node.js，重新打开终端，再运行 `node --version`。
- 安装依赖失败：连接稳定网络后，在项目文件夹重新运行 `npm ci`。
- 网页打不开：确认运行 `npm run dev` 的终端仍然开着，并使用终端显示的 Local 地址。
- 展示结束：在运行服务的终端按 `Ctrl+C` 即可停止。
